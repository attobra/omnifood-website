{
  "name": "omnifood-website",
  "version": "1.0.0",
  "description": "This will load the omnifood website",
  "scripts": {
    "start": "harp server --port $PORT"
  },
  "dependencies": {
    "harp": "*"
  }
}
